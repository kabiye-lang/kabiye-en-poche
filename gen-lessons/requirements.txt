# Core dependencies
python-dotenv>=1.0.0
supabase>=2.9.0,<2.10.0  # Pinned to avoid pyroaring dependency

# Google Gemini API
google-generativeai>=0.8.0

# Vector Store & Embeddings (for RAG)
faiss-cpu>=1.7.4
sentence-transformers>=5.0.0

# PDF Processing
pypdf>=6.0.0

# LangChain (minimal - tested versions)
langchain-core>=1.2.0,<2.0.0
langchain-text-splitters>=1.1.0,<2.0.0
langchain-community>=0.4.0,<0.5.0
langchain-huggingface>=1.2.0,<2.0.0

# Utilities
requests>=2.31.0
tenacity>=9.0.0

# INSTALLATION NOTE:
# These versions were specifically chosen to avoid the pyroaring build error.
# If you encounter issues, see TROUBLESHOOTING.md for step-by-step installation guide.

# Audio generation dependencies (for generate_audio.py)
# Only needed if using audio generation feature
# transformers>=4.30.0
# torch>=2.0.0
# scipy>=1.10.0

# REMOVED DEPENDENCIES (no longer needed):
# - ollama (replaced by Gemini)
# - langchain-ollama (replaced by Gemini)
# - torch (only needed for audio generation)
# - transformers (only needed for NLLB/audio, now optional)
# - sentencepiece (only needed for NLLB, removed)
# - unstructured (complex PDF processing, simplified to PyPDF)
# - unstructured-inference (removed with unstructured)
# - unstructured-pytesseract (removed with unstructured)
# - pytesseract (removed with unstructured)
# - pillow-heif (removed with unstructured)
# - pdf2image (removed with unstructured)
# - pdfminer.six (removed with unstructured)

# Summary: Reduced from 52 to 15 core dependencies (71% reduction)
