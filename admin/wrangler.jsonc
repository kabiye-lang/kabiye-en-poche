{
  "$schema": "node_modules/wrangler/config-schema.json",
  "name": "kabiye-en-poche-admin",
  "compatibility_date": "2025-09-27",
  "observability": {
    "enabled": true
  },
  "assets": {
    "not_found_handling": "single-page-application",
    "directory": "./dist"
  },
  "compatibility_flags": [
    "nodejs_compat"
  ],
  // Top-level config is used for local `wrangler dev` only.
  // CI always deploys with --env staging or --env production.
  // -------------------------------------------------------------------------
  // Per-environment vars are set via GitHub Environment secrets/vars.
  // -------------------------------------------------------------------------
  "env": {
    "staging": {
      "name": "kabiye-en-poche-admin-staging"
      // "routes": [
      //   {
      //     // TODO: replace with actual staging custom domain
      //     "pattern": "admin-staging.kabiye-en-poche.app",
      //     "custom_domain": true
      //   }
      // ]
    },
    "production": {
      "name": "kabiye-en-poche-admin"
      // "routes": [
      //   {
      //     // TODO: replace with actual production custom domain
      //     "pattern": "admin.kabiye-en-poche.app",
      //     "custom_domain": true
      //   }
      // ]
    }
  }
}